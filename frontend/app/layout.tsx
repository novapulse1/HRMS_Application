import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Nova Pulse HRMS — Next-Gen Multi-Tenant Enterprise HR Platform",
  description:
    "Next-Generation Enterprise Human Resource Management System with precision payroll, biometric attendance, and zero-trust multi-tenant architecture.",
  icons: {
    icon: "/nova-icon.png",
    shortcut: "/nova-icon.png",
    apple: "/nova-icon.png",
  },
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
